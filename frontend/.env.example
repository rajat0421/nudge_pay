# Base URL of the NudgePay backend API (see ../backend). Defaults to
# http://localhost:4000/api/v1 if unset — only needed when the backend runs
# somewhere else (a deployed URL, a different port, etc).
VITE_API_URL=http://localhost:4000/api/v1
